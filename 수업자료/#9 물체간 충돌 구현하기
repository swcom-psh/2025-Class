물체간의 충돌을 아래와 같이 구현할수도 있습니다~
참고로
물체.enabled = False  라는 코드를 활용하면
물체를 비활성화 할 수 있습니다.


from ursina import *

app = Ursina()
EditorCamera()


cube = Entity(model = 'cube', scale = 5, position = (0,0,0), color = color.red, collider='box')
ball = Entity(model='sphere', scale=2, position=(0,20,0), color=color.blue, collider='sphere')

#아래는 몇가지 애니메이션인데,, 참고하고 활용해도 좋습니다.

cube.animate('y', 3, duration=1) # y축으로 3만큼 올라가는 애니메이션
cube.animate_rotation((0,180,0), duration=2) # y축으로 180도 회전하는 애니메이션
cube.shake(magnitude=2, duration=10) # 흔들리는 애니메이션

def update():
    ball.y -= time.dt * 5  # 공이 아래로 떨어집니다.
    # 만약 ball이 떨어지다가 cube와 충돌하면
    if ball.intersects(cube).hit:
        ball.y = 20  # 공의 위치를 다시 위로 올립니다.
        ball.color = color.random_color()  # 공의 색상을 랜덤으로 변경
    

app.run()

